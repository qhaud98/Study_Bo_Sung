class Weather {
  final double temp;
  final int tempMin;
  final int tempMax;
  final String weatherMain;
  final int code;

  Weather({this.temp, this.tempMin, this.tempMax, this.weatherMain, this.code});
}
